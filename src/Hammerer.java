public class Hammerer extends Player
{
    
    public Hammerer(int attackVal, int defenseVal,
        int currentHealthVal, String name)
	{
		super(attackVal, defenseVal, currentHealthVal, name);
    }
}
